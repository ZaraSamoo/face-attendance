# Face-Attendance-with-Face-Recognition
Streamlit app for a face attendance system built with OpenCV and the face_recognition library. Run with:

```
streamlit run "Face Detection Algorithm/app.py"
```
